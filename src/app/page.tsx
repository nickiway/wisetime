"use client";
import Button from "@/components/ui/Button";

const Home = () => {
  return (
    <main>
      <p>Home page</p>
      <Button
        clickHandler={() => {}}
        cutomClasses="bg-blue-500 text-white"
        label="Click me"
      />
    </main>
  );
};

export default Home;
